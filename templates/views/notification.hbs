{{>navbar}}
<div class="container">
    <div class="row">
        <div class="col-md-12">
          <div class="offset-md-3 col-md-6">
            {{#each notification}}
           <div class="notification">
             {{message}}
           </div> 
           {{/each}}
          </div>
        </div>
    </div>
</div>